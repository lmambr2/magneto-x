# Proprietary binaries not stored in this public repo

`Magmotor` and `MagnetoWifiHelper` were present on the stock printer and in
Peopoly's public `magnetox-os-update` package, but are **not** kept in this
repository (project policy).

Obtain from:
- Peopoly `mypeopoly/magnetox-os-update` (if still published)
- A private local backup / TF image
- Your machine under `~/auto-uuid/`
