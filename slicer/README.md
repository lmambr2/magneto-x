# Slicer assets (magneto-x)

| Path | Purpose |
|------|---------|
| [`orca/`](orca/) | OrcaSlicer machine G-code + process notes for the modern config package |

Printer firmware macros live in [`config/macros.cfg`](../config/macros.cfg) (`PRINT_START`, `PRINT_END`, `FULL_CALIBRATE`).
