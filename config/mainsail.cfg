# Magneto-safe Mainsail client config
#
# Ships virtual SD, pause_resume infrastructure, CANCEL_PRINT, and layer-pause
# helpers. PAUSE / RESUME are intentionally omitted — macros.cfg owns those
# (stock Peopoly + stock mainsail.cfg both define them and break each other).
#
# Derived from Mainsail client macros (Alex Zellner, GPL-3.0), PAUSE/RESUME
# sections removed for Magneto X.

[virtual_sdcard]
path: ~/printer_data/gcodes
on_error_gcode: CANCEL_PRINT

[pause_resume]
# recover_velocity: 50.

[display_status]

#####################################################################
# Cancel (Mainsail / Fluidd)
#####################################################################

[gcode_macro CANCEL_PRINT]
description: Cancel the running print
rename_existing: CANCEL_PRINT_BASE
gcode:
    {% set client = printer['gcode_macro _CLIENT_VARIABLE']|default({}) %}
    {% set allow_park = client.park_at_cancel|default(false)|lower == 'true' %}
    {% set retract = client.cancel_retract|default(5.0)|abs %}
    {% set park_x = "" if (client.park_at_cancel_x|default(none) is none)
              else "X=" ~ client.park_at_cancel_x %}
    {% set park_y = "" if (client.park_at_cancel_y|default(none) is none)
              else "Y=" ~ client.park_at_cancel_y %}
    {% set custom_park = park_x|length > 0 or park_y|length > 0 %}
    {% if (custom_park or not printer.pause_resume.is_paused) and allow_park %}
        _TOOLHEAD_PARK_PAUSE_CANCEL {park_x} {park_y}
    {% endif %}
    _CLIENT_RETRACT LENGTH={retract}
    TURN_OFF_HEATERS
    M106 S0
    {client.user_cancel_macro|default("")}
    SET_PAUSE_NEXT_LAYER ENABLE=0
    SET_PAUSE_AT_LAYER ENABLE=0 LAYER=0
    CANCEL_PRINT_BASE

#####################################################################
# Layer pause helpers (call into macros.cfg PAUSE — not redefined here)
#####################################################################

[gcode_macro SET_PAUSE_NEXT_LAYER]
description: Enable a pause after the next layer change
gcode:
    {% set pause_next_layer = printer['gcode_macro SET_PRINT_STATS_INFO'].pause_next_layer %}
    {% set ENABLE = params.ENABLE|default(1)|int != 0 %}
    {% set MACRO = params.MACRO|default(pause_next_layer.call, True) %}
    SET_GCODE_VARIABLE MACRO=SET_PRINT_STATS_INFO VARIABLE=pause_next_layer VALUE="{{ 'enable': ENABLE, 'call': MACRO }}"

[gcode_macro SET_PAUSE_AT_LAYER]
description: Enable a pause at a given layer number
gcode:
    {% set pause_at_layer = printer['gcode_macro SET_PRINT_STATS_INFO'].pause_at_layer %}
    {% set ENABLE = params.ENABLE|int != 0 if params.ENABLE is defined
               else params.LAYER is defined %}
    {% set LAYER = params.LAYER|default(pause_at_layer.layer)|int %}
    {% set MACRO = params.MACRO|default(pause_at_layer.call, True) %}
    SET_GCODE_VARIABLE MACRO=SET_PRINT_STATS_INFO VARIABLE=pause_at_layer VALUE="{{ 'enable': ENABLE, 'layer': LAYER, 'call': MACRO }}"

[gcode_macro SET_PRINT_STATS_INFO]
rename_existing: SET_PRINT_STATS_INFO_BASE
description: Layer-pause hooks for Mainsail/Orca
variable_pause_next_layer: { 'enable': False, 'call': "PAUSE" }
variable_pause_at_layer: { 'enable': False, 'layer': 0, 'call': "PAUSE" }
gcode:
    {% if pause_next_layer.enable %}
        {pause_next_layer.call}
        SET_PAUSE_NEXT_LAYER ENABLE=0
    {% elif pause_at_layer.enable and params.CURRENT_LAYER is defined and params.CURRENT_LAYER|int == pause_at_layer.layer %}
        {pause_at_layer.call}
        SET_PAUSE_AT_LAYER ENABLE=0
    {% endif %}
    SET_PRINT_STATS_INFO_BASE {rawparams}

#####################################################################
# Shared client helpers
#####################################################################

[gcode_macro _CLIENT_RETRACT]
description: Retract filament (used by cancel)
gcode:
    {% set client = printer['gcode_macro _CLIENT_VARIABLE']|default({}) %}
    {% set length = params.LENGTH|default(client.retract|default(1.0))|float %}
    {% set speed = params.SPEED|default(client.speed_retract|default(35.0))|float %}
    {% set use_fw = client.use_fw_retract|default(false)|lower == 'true' %}
    {% if printer.extruder.can_extrude %}
        {% if use_fw and printer.firmware_retraction is defined %}
            G10
        {% else %}
            M83
            G1 E-{length} F{speed * 60}
        {% endif %}
    {% endif %}

[gcode_macro _CLIENT_EXTRUDE]
description: Unretract filament
gcode:
    {% set client = printer['gcode_macro _CLIENT_VARIABLE']|default({}) %}
    {% set length = params.LENGTH|default(client.unretract|default(1.0))|float %}
    {% set speed = params.SPEED|default(client.speed_unretract|default(35.0))|float %}
    {% set use_fw = client.use_fw_retract|default(false)|lower == 'true' %}
    {% if printer.extruder.can_extrude %}
        {% if use_fw and printer.firmware_retraction is defined %}
            G11
        {% else %}
            M83
            G1 E{length} F{speed * 60}
        {% endif %}
    {% endif %}

[gcode_macro _TOOLHEAD_PARK_PAUSE_CANCEL]
description: Park toolhead for pause/cancel
gcode:
    {% set client = printer['gcode_macro _CLIENT_VARIABLE']|default({}) %}
    {% set thr = printer.toolhead %}
    {% set x = params.X|default(client.custom_park_x|default(thr.axis_maximum.x / 2)) %}
    {% set y = params.Y|default(client.custom_park_y|default(thr.axis_minimum.y + 5)) %}
    {% set dz = client.custom_park_dz|default(2.0)|float %}
    {% set z_speed = (client.speed_hop|default(15.0)|float) * 60 %}
    {% set move_speed = (client.speed_move|default(100.0)|float) * 60 %}
    {% if "xyz" in thr.homed_axes %}
        G91
        G1 Z{dz} F{z_speed}
        G90
        G1 X{x} Y{y} F{move_speed}
    {% elif "xy" in thr.homed_axes %}
        G90
        G1 X{x} Y{y} F{move_speed}
    {% endif %}
