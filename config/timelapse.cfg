# Timelapse macros — package stub (so [include timelapse.cfg] resolves offline).
#
# On a real host with moonraker-timelapse installed, replace this file with:
#   ln -sfn ~/moonraker-timelapse/klipper_macro/timelapse.cfg \
#          ~/printer_data/config/timelapse.cfg
#
# This stub intentionally defines no TIMELAPSE_* macros. Using slicer layer
# G-code like TIMELAPSE_TAKE_FRAME without the real component will still error
# (correct — install the component first).
