# MCU serial / CAN identities — REPLACE with values from your machine.
#
# On a running host:
#   ls /dev/serial/by-id/
#   ~/klippy-env/bin/python ~/klipper/scripts/canbus_query.py can0
#
# Or use Peopoly's magneto-manager auto-uuid helpers if present.

[mcu]
# BTT Octopus Pro 1.1 (STM32H723 / H732) via USB
serial: /dev/serial/by-id/usb-Klipper_stm32h723xx_CHANGE_ME-if00
# restart_method: command

[mcu MAG_TOOL]
# Lancer toolhead RP2040 over CAN (via Linux Hub USB-CAN)
canbus_uuid: CHANGE_ME
