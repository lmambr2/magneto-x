# =============================================================================
# Beacon / BTT Eddy — notes only (NOT stock Lancer load-cell).
# =============================================================================
# Stock Magneto: [probe] PE12 + [magneto_load_cell] — leave that unless converting.
#
# === Beacon 3D (USB) — use the real package config ===
#   ./os/install-beacon.sh
#   [include optional/beacon.cfg]          # fill serial + offsets
#   see optional/beacon_activate.md
#   https://docs.beacon3d.com/quickstart/
#
# === BTT Eddy (I2C / eddy current) — not first-class here ===
# Upstream: Klipper Config_Reference probe_eddy_current
#
# [probe_eddy_current btt_eddy]
# sensor_type: btt_eddy
# i2c_mcu: ...
# i2c_bus: ...
# x_offset: ...
# y_offset: ...
#
# Either alt probe path requires removing/commenting stock [probe] PE12 and
# typically [magneto_load_cell] so Z endstop ownership is unambiguous.
