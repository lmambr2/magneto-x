# Validation artifacts

| Path | In git? | Purpose |
|------|---------|---------|
| `S3_HARDWARE_REPORT.template.md` | **Yes** | Empty PR-V1 / S3 checklist |
| `live-*`, `*.json` with machine data | **No** (gitignore) | Local captures; redact before any share |

Fill a copy of the template on real hardware before claiming a **v1** release tag (DESIGN PR-V1).
